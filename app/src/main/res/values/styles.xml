<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="android:Theme.Holo.Light.DarkActionBar">
        <!-- This removes the icon from the action bar -->
        <item name="android:icon">@android:color/transparent</item>
    </style>

</resources>
